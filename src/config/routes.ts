export const ROUTES = {
  landing: '/',
  app: {
    root: '/app',
    dashboard: '/app/dashboard',
    reports: '/app/reports',
    chat: '/app/chat',
    logs: '/app/logs',
    profile: '/app/profile',
    settings: '/app/settings',
  },
} as const;
